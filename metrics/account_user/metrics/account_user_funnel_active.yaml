id: account_user_funnel_active
name: Account User Funnel - Active
description: Users with activity in the past 30 days.
product: account_user
category: funnel
status: active
freshness: daily
grain: point_in_time
definition:
  business: Users actively using the platform.
  technical: Count of users with last_activity >= 30 days ago.
source: PRODUCT_TABLES
sql_template: SELECT COUNT(DISTINCT USER_ID) FROM USER_ACTIVITY WHERE LAST_LOGIN >=
  DATEADD(DAY, -30, CURRENT_DATE())
tags:
- funnel
- funnel-stage
- engagement
maintainer: product-insights@alteryx.com
last_updated: '2026-08-05'
version: '1.0'
