id: account_user_funnel_user_lifecycle_active
name: Account User - Funnel User Lifecycle - Active
description: Active account users
product: account_user
category: funnel_user_lifecycle
status: active
freshness: daily
grain: point_in_time
definition:
  business: Active account users
  technical: Count of distinct entities meeting active criteria from account user
    data sources.
source: ACCOUNT_USER_METRIC_VIEW
tables:
- DISCOVERY_PRODUCT_MANAGEMENT.METRIC_STORE.ACCOUNT_USER_AT
sql_template: "WITH base_metrics AS (\n    SELECT\n        DISTINCT CASE\n       \
  \     WHEN ACTIVE IS NOT NULL THEN 1\n            ELSE 0\n        END AS is_active\n\
  \    FROM DISCOVERY_PRODUCT_MANAGEMENT.METRIC_STORE.ACCOUNT_USER_AT\n    WHERE CREATED_AT\
  \ >= DATEADD(day, -30, CURRENT_DATE())\n)\nSELECT\n    COUNT(*) AS active\nFROM\
  \ base_metrics\nWHERE is_active = 1"
tags:
- funnel_user_lifecycle
- funnel
- conversion
- engagement
maintainer: product-insights@alteryx.com
last_updated: '2026-08-04'
version: '1.0'
